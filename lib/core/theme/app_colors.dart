import 'package:flutter/material.dart';

class AppColors {
  static const primaryStart = Color(0xFF5B5FFF);
  static const primaryEnd = Color(0xFF00C6A7);

  static const backgroundLight = Color(0xFFF7F9FC);
  static const backgroundDark = Color(0xFF0E1621);

  static const cardLight = Colors.white;
  static const cardDark = Color(0xFF182230);

  static const textPrimaryLight = Color(0xFF1A1A1A);
  static const textPrimaryDark = Colors.white;

  static const border = Color(0xFFE0E0E0);
}
